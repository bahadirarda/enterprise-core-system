/** @type {import('next').NextConfig} */
const nextConfig = {
  // Eğer bir yerde sabit port ile yönlendirme varsa, process.env.PORT veya .env.ports'tan alınacak şekilde güncellenebilir.
};

export default nextConfig;
